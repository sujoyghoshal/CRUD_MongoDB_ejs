<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
   <form action="/updateData" method="post">
    <table>
        <tr><td>username :</td><td><input type="text" value="< %=data.username%>" name="username"></td></tr>
        <tr><td>password :</td><td><input type="text" value="< %=data.password%>" name="password"></td></tr>
    </table>
    <input type="hidden" name="id" value="< %=data._id%>">
    <input type="submit">
   </form>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update User Data</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        h1 {
            background-color: #333;
            color: white;
            padding: 20px 0;
            margin: 0;
        }

        .container {
            max-width: 400px;
            margin: 20px auto;
            padding: 20px;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ccc;
            padding: 10px;
        }

        th {
            background-color: #333;
            color: white;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        input[type="submit"] {
            background-color: #333;
            color: white;
            border: none;
            border-radius: 5px;
            padding: 10px 20px;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background-color: #555;
        }
    </style>
</head>
<body>
    <h1>Update User Data</h1>
    <div class="container">
        <form action="/updateData" method="post">
            <table>
                <tr>
                    <th>Username</th>
                    <th>Password</th>
                </tr>
                <tr>
                    <td>Username:</td>
                    <td><input type="text" value="<%= data.username %>" name="username"></td>
                </tr>
                <tr>
                    <td>Password:</td>
                    <td><input type="text" value="<%= data.password %>" name="password"></td>
                </tr>
            </table>
            <input type="hidden" name="id" value="<%= data._id %>">
            <input type="submit" value="Update">
        </form>
    </div>
</body>
</html>


